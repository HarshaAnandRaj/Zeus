# LMB3: fresh fixed-candidate body qualification

New campaign, not a repaired LMB2. Its independent-audit rejection remains VOID
and learner-history correction remains a functional negative. Instrument validation
must PASS across all256 exposed demonstration-source bodies before preparation.

Declare all four final LMB2 demonstration checkpoints as unqualified fixed
candidates, with their complete optimizer/input/log twin identities. This choice
is motivated by exposed LMB2 results and is therefore not an untouched replication
of its arm comparison. No best-parent selection, update, further data aggregation,
claim of learner-history benefit or retrospective qualification occurs. Reuse
original initial and LMB1 warm policies as explicit baselines; all hashes freeze
in the new manifest before held-out generation. The old sources stay immutable.

Fresh128 ecologies213113000..213113127, inspection side floor(index/2)%2, original
fourth-body default energy and256 raw stochastic decisions. Source remains actual
three8-step public bodies with supplied inspection, slow state carried and fast
state reset at body boundaries. Both inherited and empty modes for every parent.
Execute every sampled action until actual death or horizon. Action RNG213413000
+1000*trial+index is paired across conditions. No private truth, teacher action,
mask, online optimizer, hidden-state reset within the body or recovery override.

Frozen whole-arm gates retain the original thresholds: every parent/mode
survival>=90%, mean positive feeds>=16, actual tool-increasing repairs>=.5;
pooled survival gain over initial>=.50 with positive lower95% paired bootstrap
bound. Four candidate parents jointly qualify or fail. Warm baselines are
reported and do not replace initial or select parents. Memory regression uses
fresh256 ecologies213123000 and all side/quality cells, plus synthetic paired512
inputs213223000+delay at delays64/128. Full/erase/opposite content controls,
native/delayed recall and action thresholds remain those in the old motor gate.
Regression RNG213423000+trial, synthetic adds10000+1000*trial+delay.
Bootstrap10000 draws, seed213513000, shared adjacent ecology pairs and parents.
Survival intervals are descriptive; component/regression results cannot rescue
a failed full gate. PASS/FAIL after audit; corruption, missing coverage, source or
replay defects VOID. No UNDECIDED or post-exposure parameter/budget/bar changes.

Audit complete public source and physical trajectories. Reconstruct input tensors
and training batch identities, check all four old demonstration optimizer/log/input
twins, frozen writer/reader and actual motor weight changes, and tie all collection
phase hashes to the validated instrument receipt. No independent Adam claim.
The new dual instrument computes h/z from public beginning, samples independently,
requires exact full PyTorch CPU float32 trajectory, and checks local NumPy neural
mathematics at every transition with probability<2e-5,state<1e-4. Shared primitive
operators are explicit; this does not prove cross-engine portability. Saved states
are assertions only. Native and synthetic source storage, quality decoder and
query controls are audited too; dead fast activity erased at synthetic query has
no path to its protected slow state or evaluated outputs.

Commit runner/auditor/tests/protocol/dependencies before prepare, with exclusive
JSON prevalidation and complete trace streams. Audit receipt binds manifest,
endpoint/verdict/report, source/public trace bytes and exact four candidate hashes.
Mechanics fixtures are exposed or separately declared seeds, not fresh endpoint
evidence. PASS earns preparation of a new known-memory scarce-birth transfer
protocol; LBT1's original LMB2 dependency remains closed. It earns no own-action
acquisition, selective memory, long repair-dependent maintenance or complete pillar.
