# LMB2 audit rejection: postmortem before any qualification

Frozen fresh endpoint calculations select demonstration with motor PASS; every
learner_history survival cell FAILS and its data-source attribution FAILS. Original
NumPy audit stops at its probability bound in a parent0 demonstration training-
collection source. Thus no audited motor PASS exists and LBT1's conditional gate
remains closed. All original source, outputs and2e-5/state1e-4 rules stay unchanged.

New diagnostic source freezes before locating that observed rejection. Iterate
parent0 demonstration's existing source snapshots/recorded collection bodies in
original order. Invoke frozen replay unchanged and catch only the observed line42
probability assertion. Report its exact phase/case/tick/error and checkpoint/trace
identity. Reproduce the complete phase collection with the primary frozen float32
code and compare exact logical data hashes; this is a provenance diagnosis, not an
independent qualifying audit. Also compare one NumPy step anchored to actual recorded
previous state with the rejected accumulating reconstruction. No action changes,
edited tolerance, source hotfix, endpoint rerun or qualifying PASS can follow.

If local math is accurate and complete primary collection is exact, this supports
finite-precision accumulation as the source of rejection. It does not retroactively
relax the frozen bound, verify every other trace, guarantee hardware portability or
demonstrate resilience. Future use needs a fresh protocol/ruler, prospectively
specified complete hardware-trajectory and independent math/physics coverage.
If source or local math is wrong, retain the error and diagnose the actual mismatch.
The full roadmap remains active; failed numerical integrity cannot be silently
turned into a successful handoff.
