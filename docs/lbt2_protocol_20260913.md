# LBT2: fresh known-memory scarce-birth actuator transfer

Fresh follow-up preparation earned by LMB4's independently audited controllerPASS,
source4aab291; selected grounded by its frozen priority. Grounding attributionFAIL
stays explicit. Old LBT1 is unlaunched on its unearned LMB2 prerequisite and is not
reopened. Source qualified models are reused openly; new qualification data are
fresh, no fit or old endpoint repair. Full-roadmap authorization remains binding.

## Source and physical roles

All four qualified models, unchanged during operation. Complete audited-source
helper binds original source manifest/code, independent receipt/report, all16
data/endpoint artifact hashes,8 exact final model/head/optimizer/log twin pairs,
64 collection/source phases and all candidate/head identities. New manifest
binds that qualified source and the audited scarce-resource reference ruler;
all original calibration artifact/code identities are checked. Model prediction
heads remain passive source artifacts and never choose actions.

128 fresh ecologies218113000..127 at EACH equally represented later birth energy
.12/.20/.35/.85. Source inspected side=floor(index/2)%2, balanced32 per side/quality
cell. Use the unchanged BirthResourceEcology: body0 original energy.85; bodies1/2/3
at the profile's energy, all other physical/RNG state preserved. Generate three
actual8-step public source bodies: body0 two supplied moves, INSPECT, then WAIT;
bodies1/2 WAIT. Even scarce windows must remain alive, with all real costs/damage
retained. Derive labels from actual public inspection, never fabricate quality.
512 prepared lineages shared across models; source acquisition is engineered.

Native real fourth body: inherited actual protected slow state, fresh fast/context
at birth,256 continuous raw sampled decisions. All own public action→physics→
next observation/reward→eligible write→later read/action events are preserved.
No optimizer, teacher replacement, hidden quality/seed/snapshot input, action mask,
temperature change, per-tick reset or rollout through death. Public sensing stays
available. Sampling RNG218413000+1000*parent+index paired across profiles.
All4×4×128=2048 bodies and64 parent/profile/side/quality cells are retained.

## Frozen qualification

Each cell: survival>=90% (>=29/32), mean positive feeds>=16, actual tool-increasing
repair mean>=.5, initial source quality recall>=90%, exact source/start slow-state
identity and fast/context reset. First direction and early deaths are diagnostics,
not replacements for full operation. Descriptive Wilson95 intervals are reported;
they do not change point-threshold gates or hide a failed cell. Every profile stays
visible; no pooled/high-energy substitute or favourable-parent selection.

PASS/FAIL after independent audit; source/twin/coverage/physics/neural-replay defects
VOID. No UNDECIDED, post-exposure energy/horizon/sampling/bar change or extra fit.
Repair count here still does not prove necessity; the separately audited repair
family and unlaunched LMT1 address long necessary maintenance independently.

## Mechanics, replay and complete evidence

Commit runner/auditor/tests/protocol/dependencies before exclusive output creation
at runs/lbt2_20260913. Commands: run_known_birth_transfer2.py prepare|evaluate|
finalize, then audit_known_birth_transfer2.py. Source/calibration guards run before
directory creation, all partial outputs are preserved. Three campaign and eight
shared guard/operation/physical mechanics tests precede freeze; short development
fixtures test the instrument, not256-step qualification. Independently replay
actual scarce public source windows, all protected writes and fast boundaries.

Auditor builds from original lineages and applies only declared later energy/config
changes, comparing full factory/RNG state. Reconstruct public sources/quality labels,
native slow starts and every body from actual public beginnings. Complete PyTorch
CPU float32 GRU/linear/sampling trajectories must match exactly; independently
check local NumPy neural mathematics probability<2e-5/state<1e-4. Saved h/z remain
assertions, not trajectory resets. CheckedWorld compares every observer physical
before/after vector to replayer-owned physics and independently arranged scalar
balances at1e-12. Reconstruct initial quality head math and all summaries/cells/
Wilson intervals/verdict independently, verify unchanged models, and bind full
manifest/preparation/trace/endpoint/verdict/report/source identities in the receipt.
No independent Adam or cross-engine portability claim is made.

## Earned boundary and failure consequence

AuditedPASS earns preparation of the separately frozen agent-own acquisition/
inheritance experiment. It does not establish a native memory advantage: acute
erasure, opposite content, disabled writes and equally trained forgetting controls
remain owed under independently measured function. Later sensing stays available;
inspection savings cannot replace a failed survival contrast. Cycles preserve
declared useful information rather than forcing every component to start fresh.

TransferFAIL earns complete public-state/action/death diagnosis and, if justified,
a fresh equal-profile known-information controller-learning protocol with new
fit/check roles. Do not mistake inability to actuate known facts for a memory
storage failure or resume this zero-update endpoint with extra training. Own
acquisition, useful selection/revision, authorship, capability expectations,
intrinsic resilience/priorities/initiation/expression and integrated six-pillar/
live-session operation remain in the full roadmap.
