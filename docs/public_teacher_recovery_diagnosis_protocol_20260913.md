# Public teacher recovery: exposed-history diagnosis

Diagnostic only; LMB2 stays VOID and fresh LMB3 stays frozen. Motivated by the
literature's information/provenance warning, directly test the actual public
teacher on learner-reached histories rather than importing a privileged-state
expert explanation. Commit source/protocol before compute. No neural fitting or
new held-out data, and no fresh LMB3 artifact is opened.

All512 recorded collection bodies across4 parents,2 arms,8 phases,8 bodies.
Independently replay every actual public physical record/teacher label. At prefixes
0,32,64,96,128,160,192,224 that were reached alive, fork original physics and
continue the fixed public teacher to original total horizon256 or death. Compare
WAIT from the identical fork and report the recorded learner's eventual outcome.
Keep all reached cases, including early fatal histories; report conditional sample
counts. Unreached checkpoints are absent because of actual death, not exclusions
to improve the result. Multiple checkpoints/phases share ecologies and are not
independent trials. No confidence-based qualification or causal neural claim.

The observer restores a privileged physics snapshot solely to initialize the fork.
Teacher receives only current public observation and its safe/tool variables
derived from supplied public cue and all actually executed public consequences.
No private quality/tool/seed/snapshot enters teacher decisions. Observer tool error
is diagnostic only. This deliberately replaces later learner actions with teacher
actions in a counterfactual world; it is not an intervention used to rescue a
learned endpoint. Corrected survival cannot qualify a neural agent or prove that
the teacher is optimal; failure may reflect an already unrecoverable state.

Report exact input/source hashes, source physical steps, every fork result and
arm/checkpoint/recorded-outcome counts. Any source mismatch stops extraction; no
threshold selection, teacher edit or fresh endpoint tuning follows the diagnosis.
