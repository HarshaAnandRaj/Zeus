# LMB5 complete failure diagnosis

Read-only descriptive instrument, prepared after rawFAIL while the independent
audit runs. Execution requires that completed audit to verify FAIL, exact bound
artifact/report/source identities, 7168 bodies, eight whole fit pairs and 768
training batches. No exposed campaign source or gate changes.

Reconstruct all 512 public endpoint preparations and all bodies in balanced,
original and warm variants, including high-energy empty controls. Consume every
saved decision once, verify physical/public/reward/final-state fields and complete
counts against the audit. Retain all parent/energy/mode/side/quality/outcome groups,
including empty groups. Report energy-only/integrity-only/both deaths, actual
feeding/restoration, inspections, slow-state changes, first safe arrival, safe-patch
waiting, total action counts and first eight decisions for every body plus last
sixteen for every death. Safe side derives from public preparation only in the
observer, never enters the neural model. Empty-control teacher comparisons begin
without that cue and acquire it only from actual public inspection.

Teacher agreement describes supplied public rules, not optimality. Extra harvest
integrity loss may include starvation. No neural counterfactual, new fitting,
rescued survival, memory-benefit or pillar claim is licensed. Source must be
committed before extraction; preserve existing report on rerun. Tests use new
development fixtures, not endpoint tuning or selected failure cases.
