# LMB5 draft: learn urgent operation at fresh scarce births

Motivation: independently closed LBT2FAIL plus complete diagnosis. All2048 bodies
recall correctly and reach the safe patch in two moves, yet all512 lowest-energy
third-decision argmaxes are WAIT. Previous body training starts at energy.85;
low-energy states later in a trajectory are not equivalent to low-energy birth
with a reset fast state. No state awareness or emergence is inferred.

The proposed intervention is physical training exposure, not a hardcoded feeding
override, teacher action at evaluation, new viability budget or memory-ownership
loss. Keep the same qualified LMB4 grounded source and frozen store/quality reader.
Train existing fast/gate/reinstatement/actor parameters from real public teacher
trajectories. Prediction heads stay frozen and unused by actions: their previous
grounding-survival attributionFAIL does not justify another identical head sweep.

Compare two equally budgeted arms, balanced births and original births. Both
use the same64 source ecologies, four nominal profile slots, inherited/empty
contexts and matched minibatch indices. Balanced actual birth energies are
.12/.20/.35/.85; original actual energy is.85 in every slot. In the original arm,
slots repeat actual high-energy preparations and do not pretend to be scarce.
Each generated action and consequence is physically executed. Empty low-energy
teacher trajectories may die; retain actual deaths, mask only padding and publish
live step counts. Equal optimizer updates do not imply equal live training steps.

Proposed fixed fit:96 updates per arm/parent, batch8 with one sample per profile
slot and memory context, learning rate.003, weighted body cross-entropy with the
existing action weights, native full-memory first-direction cue weight.25,
gradient clip1 and truncation32. Forward recurrent state continues through a body;
only backward history is truncated. Independent whole checkpoint/optimizer twins
and intended module gradients are required. Frozen memory modules receive no
learning credit in this prerequisite; this is not a selective-memory learner.

Use disjoint prospective roles: training219013000, public teacher check219063000,
evaluation219113000, and separate readout/regression219123000. Full campaign
protocol must state exact rosters/counts and bind all sources before any96-update
fit or fresh evaluation. Development fixtures use992000-family seeds and short
bodies, never these campaign roles or the exposed LBT2 roster.

Proposed qualification retains LBT2 inherited gates in every fresh128-ecology
parent/energy/side/quality cell, and separately checks viable original-energy
empty-memory operation and native content/reset readout controls. Opposite banks
must come from real opposite public-source preparations with matching inspected
side, not an observer-fabricated vector. Both arms and the zero-update source
must be evaluated on the same fresh worlds/random streams. No pooled rescue.

Report exposure attribution separately from actuator qualification: paired
balanced-minus-original survival at energies.12 and.20, point effect>=.02 and
95% lower confidence bound>0, with parent/quartet resampling preserving all four
side/quality cells and cross-profile pairing. A qualified arm may lack attribution;
frozen selection priority and this distinction must be explicit. Extra original-
birth learning and zero-update references prevent crediting mere added training
to scarce-birth exposure. Finite parent/world families do not prove universal gain.

Audited controllerPASS could earn the separately frozen own-acquisition/native
inheritance experiment; qualification is not that experiment's result. All four
forgetting/content/write controls and all other roadmap obligations remain owed.
FAIL earns a complete diagnosis and a genuinely justified new mechanism, not more
updates or thresholds on the exposed endpoint. Audit defects produceVOID.

This is a draft plus development training components, not a frozen full campaign.
Complete runner, physical/neural/fit auditor, calibration and final protocol remain
necessary before campaign preparation. No campaign endpoint is launched here.
