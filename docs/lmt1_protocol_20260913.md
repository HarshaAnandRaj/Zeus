# LMT1: bounded continuous repair-dependent neural maintenance

Fresh engineered follow-up preparation earned by the repair-dependent reference
ruler's independentPASS (sourcec244372). LMB4's complete independent qualification
is additionally required before campaign preparation or endpoint exposure. No
body endpoint or fitting is authorized by a raw result alone. Native scarce-birth
memory transfer retains immediate priority; this separate maintenance campaign
must not silently replace it. All older FAIL/VOID remain closed.

## Fixed source and world roles

All four models of LMB4's frozen-priority qualified arm. Complete qualified-source
helper requires the original independent receipt, all16 endpoint/data artifact
hashes,8 exact final model/head/optimizer/log twin pairs,64 source/collection phases,
candidate/head identities and original local/full-replay bounds. Grounding
attribution is independent; itsFAIL cannot be relabelled as a gain or used to
reject an otherwise qualified controller. Bind the complete source in a new
manifest. Models frozen throughout; no optimizer, weight change or external
action selection. Passive prediction heads do not choose actions.

128 fresh ecologies217113000..127, source inspected side=floor(index/2)%2,
balanced32 per side/quality cell. Three actual public preparation bodies,8 steps
each, in the new repair family: body0 two supplied moves and INSPECT then WAIT;
bodies1/2 WAIT. Fact/target metadata are derived from the observed inspection
for audit/diagnostics; only public records enter consolidation. This supplied
acquisition is engineered and does not establish agent-own experience gathering.

Real body3 starts at energy.85/integrity.95/tool.90 and runs4096 continuous steps.
Inherited/empty slow-state starts crossed with enabled/disabled tool restoration
in every parent/world. Both contexts reset fast/previous-action/reward/done only
at birth. Empty is an explicit acute slow-state cut; eligible public writes and
sensing stay available. Tool-restoration cut changes only tool_repair=0; integrity
repair remains possible and the control identifier never enters the policy.
Physical state and continuing recurrence are preserved within each body. No
teacher, hidden quality/snapshot/seed, action mask, temperature change or reset
throughout alive operation; raw action RNG217413000+1000*parent+index paired across
all four conditions. Total2048 bodies, with every death/step retained.

## Frozen functional judgments

Each enabled parent/start-mode/source-side/source-quality cell: survival>=90%
(>=29/32), exact protected source/start identity and fast reset. Each surviving
body must perform positive actual tool restoration. In inherited starts, public
source quality recall>=90% in each cell. Empty initial recall is diagnostic,
not a demand that a forgotten fact remain available. Report feeding/repair means,
initial direction, quality scores and descriptive Wilson95 intervals; neither
pooled scores nor intervals replace a failed cell. Do not enforce the reference's
23-restoration schedule.

Every disabled-restoration body: no actual tool increase, death by598. This is
supported by the audited configured energy envelope, not inferred from one failed
controller. The global bound without restoration is E_T<=.85-.0075T+3.651934;
at598 the envelope falls below.02. No outside resources, effective tool restoration
or mid-body reset may invalidate its assumptions. No matched retrained forgetting
claim is made about the cut. Enabled inherited/empty success alone cannot establish
memory benefit; the separate native inheritance controls remain required.

PASS/FAIL after full independent audit; source/twin/coverage/replay defects VOID.
No UNDECIDED, parent filtering, horizon/energy/sampling-bar changes or extra fitting
after exposure. A failure remains visible and earns complete public-state/action/
death diagnosis before any fresh interaction-learning design.

## Mechanics, independent replay and artifacts

Commit runner/auditor/tests/protocol/dependencies before exclusive output
creation at runs/lmt1_20260913. Prepared commands: run_long_neural_maintenance.py
prepare|evaluate|finalize, followed by audit_long_neural_maintenance.py. Check
source/world prerequisites before creating a directory; preserve all partial
outputs. Shared operation is frozen-policy evaluation (`no_grad`), not a missing
training path: LMB4's actual training credit is separately audited.

Three campaign tests plus eight shared guard/operation/physical tests precede
freeze. Short development fixtures validate mechanics only, never functional
qualification. Independent auditor builds worlds from original lineages with
only the declared floor/tool-restoration changes, verifies full factory/RNG state,
reconstructs all public preparations and slow starts, replays every actual action/
public event and complete continuing state with the validated dual instrument,
and independently checks local NumPy neural math at probability<2e-5/state<1e-4.
Saved states remain assertions. CheckedWorld requires each private physical
before/after vector to match replayer-owned physics and independently arranged
scalar balances at1e-12; records never advance the world. Verify disabled energy
envelopes, initial quality math, boundaries, all2048 cases and unchanged source
weights. Recompute all cell gates/intervals independently; bind manifest, source,
preparation, trace, endpoint, verdict and report hashes in the receipt.

## Completion boundary

An auditedPASS establishes bounded long neural maintenance in this static physical
family. It earns preparation of changed-capability/recovery testing under a fresh
protocol, not six-pillar promotion or reopening of the old mouth. Own acquisition,
memory function/selectivity/revision, capability expectations, internal authorship,
intrinsic disturbances, priorities/initiation/expression and integration remain
separately owed. Effective repair is an engineered physical need; learned operation
there is not automatically an observed emergent property or phenomenological proof.
