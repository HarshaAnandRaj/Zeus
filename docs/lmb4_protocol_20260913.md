# LMB4: public-state grounding versus detached-body prediction control

Fresh engineered motor prerequisite under the full-roadmap authorization. LMB3's
audited115/128 failure and every older closed failure/VOID remain closed. Reuse
all four unqualified LMB3 models explicitly; no parent selection or old held-out
data for fitting. Bind their audited artifact/model identities in the new manifest.
This is not an untouched replication of the previous arm comparisons.

## Fixed learning mechanism and controls

Both arms have identical initial body weights and prediction heads. Current head:
Linear32→3 predicts public energy/integrity/position from continuing fast h.
Consequence head: Linear38→32,tanh,Linear32→3 predicts next changes in those
three sensors from integrated mouth vector plus actual executed-action one-hot.
No hidden quality/tool/seed/snapshot or future observation enters head inputs.
These sensor targets are engineered constraints, not emergent awareness.

Grounded auxiliary gradients reach fast state/gate/reinstatement; detached control
uses the same heads/forward targets with body features detached. Heads are trained
in both. No auxiliary actor gradient; writer and quality remain frozen. Body/cue
action losses remain the declared public teacher BC, action weights(1,8,8,4,16,16),
cue weight.25. Own-history expert labels are recorded for provenance but unused.
Auxiliary weight.25 on current3-channel MSE plus next-change3-channel MSE.

96 fixed Adam.003 updates, batch8 demonstrations plus batch8 own histories,
32-step gradient truncation preserving recurrent values. Motor/head gradient
clipping separately at1; head norms cannot rescale motor control gradients.
No further fitting after exposure. Eight source snapshots/collections at updates
0,12,...84; each collects8 raw real bodies in both arms. Aggregate own public
histories for auxiliary fitting, with actual executed actions and exact death
boundaries. Padding is inactive, target current sensors must equal input sensors,
no training after death. Same allocations/RNGs, not necessarily same lived counts:
report all live data. Four parents×two arms×two twins=16 fits,1536 update slots;
audit768 unique updates and8 exact complete model/head/optimizer/input/log pairs.

## Roles, information and physical loop

Demonstrations:128 ecologies214013000..127, both inspected sides, known/empty
birth modes balanced as prior public teacher data. Own-preparation ecologies128
at214023000..127, both sides; inherited iff floor(case_index/4)%2=0. Sequential
collection slices8*r..8*r+7 cover32 ecologies in64 cases per fit, balancing four
side/quality cells in both modes per phase. Preparation supplies actual public
inspection in body0 and eight actual steps per source body0–2. This engineered
bank is not autonomous acquisition. Evaluated/collected fourth body continues
fast recurrence, public action→physics→next observation/reward→eligible slow write
without per-tick reset, oracle, action mask, teacher replacement or online optimizer.

Head initialization RNG214043000+parent, forked from ambient RNG. Training cue
inputs214033000+update, batch8 delay1. Fresh public teacher calibration128
ecologies214063000..127, both sides, requires all256 full bodies alive. Demo batch
RNG214313000+update samples4+4 from the same demonstration set; own batch
RNG214323000+update samples8 from aggregated live histories. Raw collection RNG
214343000+100000*parent+1000*phase+case, paired across arms/twins. Heads do not
choose actions; prediction annotation uses pre-action state and executed action,
never the recorded future observation. Persist all complete public records.

Held-out128 ecologies214113000..127, inspected side floor(index/2)%2, both inherited
and empty modes for every parent. Default body energy.85 and256 raw decisions.
Original initial model and exact LMB3 parent are baseline controls, not replacement
candidates. Action RNG214413000+1000*parent+index paired across all conditions.
Native regressions256 ecologies214123000..255 both sides, content controls
full/erase/opposite; paired synthetic512 inputs214223000+delay at delays64/128.
Regression action RNG214423000+parent, synthetic adds10000+1000*parent+delay.

## Frozen judgments and conditional next scope

Wholly qualifying arm: every parent/mode survival>=90%, mean positive feeds>=16,
actual tool-increasing repair mean>=.5; original-initial pooled survival gain>=.50
and positive lower95% paired bootstrap bound; complete old native/delayed
storage/quality/action/donor/erasure regression gates unchanged. Grounded if wholly
PASS, otherwise detached if wholly PASS, otherwise none. No favourable parent,
mode or metric substitute. A repair count does not prove repair is necessary.

Grounding attribution separately requires pooled grounded-minus-detached survival
>=.02 and strictly positive lower95% bound in BOTH modes. Bootstrap10000 draws
seed214513000, resampling parents and shared adjacent ecology pairs. Function may
qualify without auxiliary attribution; do not claim grounding benefit then.
Prediction RMSE/current decoding are diagnostics only, not gates or a self-model
qualification. PASS/FAIL after audit; source/coverage/twin/replay defects VOID.
No UNDECIDED, threshold changes or post-exposure budget/seeding/parent sweeps.

## Independent evidence and completion boundary

Commit all runner/auditor/tests/protocol/dependencies before prepare. Exclusive
prevalidated JSON and exact artifact hashes preserve partial work. Independently
reconstruct public observation/action/previous-context/slow-state/active/current/
delta tensors, all batch allocations/hashes and source teacher labels. Replay512
unique raw source bodies and4096 endpoint bodies including baseline controls.
Check unchanged writer/quality, revision/actual motor/head weight movement, exact
optimizer twins and first auxiliary-credit routes. No independent Adam claim.

New dual replay computes full continuing states from public beginnings, requires
exact complete PyTorch CPU float32 trajectory and independently checks local
NumPy neural math (probability<2e-5,state<1e-4), plus public physical accounting.
Independently check both auxiliary heads at source and endpoint with1e-5 output
tolerance. Shared primitives and local scope are explicit, no cross-engine
portability claim. Saved recurrent states are assertions for whole replay, never
resets/inputs; verified states may subsequently anchor independent head math.
Audit all native/delayed content queries and bind manifest/trace/data/checkpoint/
head/verdict/report identities. Green mechanics alone is not function.

Audited qualified arm earns only preparation of a fresh known-memory scarce-birth
transfer protocol with all four parents, no older LBT1 reopening. Own acquisition,
selective utility, revision, repair-dependent long operation, authorship, internal
priorities/initiation, expression and integrated six-pillar/live session remain owed.
