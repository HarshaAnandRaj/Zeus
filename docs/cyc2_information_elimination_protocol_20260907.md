# CYC2: finite information-access elimination experiment

2026-09-07. The user authorized the proposed input comparison and delegated
elimination decisions: "We need it fail or pass ... that's where I defer it to
you." Commit this protocol and checked instrument before compute. This is a
fresh, single Class E diagnostic authorization, consumed on launch. CYC1 and
CYC-F historical verdicts are not rewritten. Retention remains closed negative.

## Binary decisions, honest scope

For each integrity-valid trained arm: PASS only if normal survival Wilson95
lower bound at256 >=.90 AND at512 >=.80. Otherwise FAIL TO QUALIFY, including
a confidence interval crossing a threshold. Publish all counts/intervals and
whether the upper bound excludes the threshold. There is no UNDECIDED funding
decision, extra draw, alternate decoder or automatic continuation. This is a
binary qualification rule, not a claim that uncertain biology or architecture
has become certain. A failure eliminates this finite recipe/input contract as
a demonstrated viable controller; it does not prove representational impossibility.

Broken integrity is INVALID / STOP, with its reason published and no automatic
rerun. It is not relabeled a scientific negative. This is the sole exception
to functional PASS/FAIL, because corrupted evidence cannot adjudicate function.

## Frozen matched conditions

All three arms use a fresh Linear49->64, Tanh, Linear64->6 policy, identical
initial tensors and minibatch order, CPU float32, deterministic Torch with one
intraop and interop thread. All parameters train; quotient tensors stay frozen.
Unused input coordinates are zero. No feature normalization or class weighting.

- A `observations_direction`: coordinates0:5 are the five current observations,
  coordinates5:48 zero, coordinate48 is an engineered sweep-direction bit.
- B `quotient_direction`: coordinates0:48 are unchanged QV1 decision_features,
  coordinate48 is the same engineered bit.
- C `quotient_only`: same quotient features, coordinate48 always zero.

The bit is explicitly assisted memory: initialize +1; before each decision
set +1 at left boundary, -1 at right boundary, otherwise retain it. It reads
actual current position on the learner's own trajectory. It is not a label,
future observation, or action override. It supplies boundary-conditioned
sweep memory and is not claimed to be self-organized or learned. This updates
direction earlier than CYC0's private variable when harvesting/regulating at
a wall; those teacher actions ignore direction, so labels are unchanged.
Verify exact teacher action equivalence before training. On a learner's path,
the bit follows actual visited boundaries, never a prerecorded teacher tape.

Training data: reuse the complete CYC1 twin-A32768 examples from teacher worlds
202661000..202661063,512ticks. Verify exact twin artifacts, parent state SHA
`bae914c2095c0b55fb334aa4cbb4e42193f449cc4d51a672e3e6620878daada4`, frozen source
hashes and dataset shapes. Reconstruct observations/bit by saved-action replay;
verify all effects and labels against the unchanged teacher and the explicit
bit controller. Preserve world-major/time-major ordering. No new demonstrations.

Every arm gets exactly100 epochs (6400 optimizer steps), batch512, AdamW
lr.0003, betas(.9,.999), eps1e-8, weight_decay.01, amsgradFalse, foreachFalse,
fusedFalse. Policy initialization seed20260971; epoch permutation uses a
separate CPU Torch Generator seed20260971+1000+zero_based_epoch. This is five
times CYC1's fixed fitting budget, chosen before CYC2 exposure to give the
positive control a fuller attempt. No validation selection or extension.
Two sequential independent twins fit all three arms afresh. Initial/final
tensors, optimizer state, rows, final teacher-data logits and every endpoint
trace must match exactly. Save final full-dataset confusion and class recalls;
training accuracy is diagnostic and cannot rescue failed survival.

Evaluation:128 new worlds, seeds202671000..202671127, greedy argmax lowest
index on ties, horizon512, no policy sampling. All arms share worlds. These
seeds were absent from repository protocols/instruments when frozen. Verify
disjointness from training worlds; do not claim independent policy-training
seeds from exact twins. Wilson intervals (z1.959963984540054) quantify variation
across worlds for this one fitted policy, not robustness across initializations.
Before model endpoints, run the unchanged scripted sweep on all128 worlds;
it must clear both survival bars, else INVALID / STOP. Do not change the worlds.

At every endpoint decision, retain actual previous observation/action in the
frozen quotient update; never mutate underlying state when constructing inputs.
Save every observation, state, direction, feature vector, logits, action/effect,
age, survival256/512, actual movement and terminal cause. Reuse the frozen
CYC-F closure definition descriptively only. Evaluate all three normal arms
regardless of A's result; unexpected successes still deserve to be recorded.

For B additionally evaluate zero-bit and flipped-bit full trajectories on the
same128 worlds. The hidden boundary-memory variable evolves normally; only
the current policy input is zeroed/flipped. Save matched zero/flipped action
choices on B's normal trajectory without altering it. This tests assistance
dependence directly, without assuming a difference between trained arms is
the same intervention. Both controls are evaluated even if B fails.

## Assistance-repair decision and elimination map

Binary repair demonstration PASS requires A PASS, B PASS, C FAIL, and paired
normal-B minus each zero/flipped-B survival512 difference lower95 >=.30.
Otherwise the claim "this assistance repairs a viable controller" FAILS TO
QUALIFY; it is not silently weakened to flips, age, accuracy or geometry.
Bootstrap10000 whole-world draws, NumPy PCG64 seed20260972, shared across both
contrasts, linear percentile95. Both comparisons must clear; zero differences
or missing benefit cannot be rescued by action-flip rates. This narrow test
does not claim all possible memory representations are inadequate.

- A fails: this direct-input positive-control recipe failed. Do not blame the
  quotient from that comparison. Any B/C success remains an engineering result.
  Stop further cloning variants under this authorization.
- A passes, B fails: supplying this bit did not produce viable quotient control.
  Eliminate the bit-only repair proposal; other compressed-information or fitting
  explanations remain possible. Stop further bit-only training.
- A and B pass, C fails: the assisted representation works; assistance counts
  as a demonstrated causal repair only if both acute survival contrasts clear.
  If they do not, close the causal repair claim as failed qualification.
- C passes: categorical inability of this interface to execute viable control
  is contradicted on this endpoint. A direction-bit addition is not established
  as necessary for the demonstrated function; do not add it on that premise.
- None pass: all three100-epoch input/training recipes are eliminated as
  demonstrated viable controllers. No epoch, seed, decoder or reward rescue.

Every outcome ends this experiment. A fresh proposal would require a distinct
mechanism and authorization; this registration purchases no subsequent run.
The result can guide a proposal without granting it permission to execute.

## Integrity and evidence

Refuse existing output directory `runs/cyc2_20260907`. Save source manifest
for this protocol/instrument/tests and all imported core/training/tool source,
CYC1 parent-data files, Git commit, complete configuration and runtime versions.
Verify named source hashes before/after phases (new unrelated files are not
source mutation). Verify frozen quotient identity before/after training/eval.
Stop and preserve partial evidence on any nonfinite/config/source/twin defect.
Run synthetic mechanics checks only before registered compute. Independently
reload twins, replay saved actions, verify balances/input construction and
recompute endpoint qualification. Publish all failures and all six emergence
questions: this is engineered control assistance, not discovery or a pillar pass.
