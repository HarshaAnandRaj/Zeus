# LMT1 complete maintenance failure anatomy

Read-only observer conditional on complete independent LMT1 auditPASS confirming
functionalFAIL. Commit source/protocol before extraction. Cover all2048 bodies,
all parents, both inherited/empty starts and enabled/disabled tool-restoration
conditions, every death and every actual public/physical step. Bind the original
complete audit, source, manifest, preparation, trace, endpoint, verdict and report.

Independently reconstruct every128 public source preparation and each evaluated
physical body. Check exact public consequences and observer physical before/after
vectors with scalar balance1e-12. Complete neural replay is supplied by the prior
audit; this observer makes no additional neural/optimizer portability claim.
Recorded h/z/probabilities remain observations, not substituted states.

Record each body's first sampled/argmax decision and actual elapsed first safe
patch, positive net feeding, effective tool restoration, inspection and slow-state
change. Count all actions, interior actions, safe-patchWAIT, unsafe-patchHARVEST,
effective and ineffective MAINTAIN, actual feeding and extra integrity loss on
HARVEST. Extra harvest integrity loss is not automatically contamination because
starvation also damages integrity. Compare all sampled actions/argmaxes with the
existing fixed public teacher, using the supplied source cue only for inherited
starts and actual action/public consequences afterward. Teacher assumes normal
tool-restoration capacity; the disabled-cut flag never enters its decisions.

Verify every endpoint feeding/repair/inspection/extra-harvest-loss/final-state
summary. Retain elapsed last feeding/restoration gaps, actual joint energy/integrity
death categories, terminal position/action/energy/integrity/tool and full last16
public/neural decision records for every death. Aggregate all parent/start/cut/
source-side/quality/outcome groups including empty groups; do not pool away cells.

Counts of repairs do not establish sufficient feeding, recovery or resilience.
Slow-state change may be appropriate public acquisition rather than forgetting.
Teacher agreement is observational, not optimality or recovered viability. The
reduced efficiency world differs from training; failure before4096 cannot be
attributed solely to long recurrent operation. No replacement action, neural
intervention, new counterfactual body, optimizer or rescue runs in this diagnosis.

This may justify a separately frozen interaction-learning or fresh-controller
transfer experiment after its own prerequisites. LMT1 remainsFAIL, source/gates/
world/horizon/sampler unchanged. Newly fitted LMB5 models require their separate
qualification and cannot inherit this test's verdict. All other roadmap obligations
and the native memory-benefit/forgetting controls remain owed.
