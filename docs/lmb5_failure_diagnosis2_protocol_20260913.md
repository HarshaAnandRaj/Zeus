# LMB5 descriptive diagnosis version 2

The original diagnosis at c292fbb is rejected and preserved. It reconstructed all
7168 bodies but its final assertion incorrectly equated body decisions with the
full audit decision count, which includes native readout queries. No COMPLETE
report was produced. The campaign audit and functionalFAIL remain unchanged.

Version2 repeats the entire original descriptive coverage and physical replay,
with the same body groups, public teacher comparisons, first/death windows and
prerequisite evidence guards. Its count invariant explicitly requires

    body steps + (2 fitted arms * 4 parents * 3 controls * 512 queries)
        = audit decisions.

Thus body steps are1540015 and separate query decisions12288 in the existing
bound audit. These counts are instrument accounting, not new functional gates.
All old diagnosis scope limits apply: no fitting, counterfactual, rescue, source
qualification, optimality or pillar claim. Publish a distinct version2 report,
commit source before extraction, and retain the original failed source and log.
