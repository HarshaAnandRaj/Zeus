# Retention Phase exit review: closed with a negative result

2026-09-07 local date. **Execution plan complete; current retention phase
closed negative. No successful retention handoff and no higher pillar opens.**

The fixed objective remains machine-native functional self-organization,
operationally defined by all six pillars. It is not human imitation and does
not license phenomenological claims. The completed work rules out the current
licensed routes as demonstrations of that objective; it does not rule out
machine consciousness in general or redefine the goal around easier metrics.

## SEL1: FAIL at selection, exact twins

After explicit approval, both empty canonical directories were checked for
hidden entries and reparse paths, documented in history, and removed without
recursive deletion. The unchanged runner executed generation and separate-
process evaluation for twin A, then twin B, then one joint finalization.
Progress lines alone were monitored until both twins completed.

Each twin accepted 123 of 128 observed-token proposals. All 8,192 selection
observations reproduce exactly. Only 32 candidates were sampled in at least
eight distinct blocks; 91 lacked that support. Two entries met the frozen
positive-utility lower bound, versus a mandatory minimum of eight. Selection
was independently recomputed from the saved rows with the exact same result.

| Frozen bar | Endpoint disposition |
|---|---|
| 1. Identity, exact twins, integrity and reload | Passed for every executed phase; canonical payload, metadata, generation and evaluation match |
| 2. At least 8 consolidated entries plus endpoint coverage | **FAIL: 2 entries**; endpoint coverage not reached |
| 3. Independent dense CE advantage over every control | Not evaluated under the registered early-stop rule |
| 4. Independent retained-versus-pool utility-fraction advantage | Not evaluated under the registered early-stop rule |

Separate evaluation processes verified the serialized payloads and stopped
at the selection failure. Calibration and endpoint exposure are zero; neither
twin has a calibration report. Do not describe unexecuted causal endpoint
controls as failed tests or infer their outcomes from selected utility.

### Direct reading of every retained entry

These are state-key/target-embedding entries with observed-token provenance,
not saved authored utterances. Both have action_origin=False.

| Memory ID | Context fragment and next target | Selection mean / 95% interval | Supporting blocks |
|---|---|---:|---:|
| 14 | "…grave gossip which marks the dwellers in\nv" → "ill" | 0.14211 / [0.04988, 0.23983] nats | 10 |
| 29 | "…he had a worse look by daylight.\n\n�" → "I" | 0.18397 / [0.08296, 0.28697] nats | 22 |

The second decoded token window ends in a replacement character. The stored
token IDs are preserved exactly; no text repair or human selection occurred.
The snippets are corpus observations, and positive selection utility is not
independent inherited function, meaningful speech, or causal authorship.

### Mandatory emergence grading

1. **Designed setup:** engineered proposal schedule, retrieval and utility
   selector. Any selection effect is an engineering result.
2. **Unprogrammed setpoint:** entry identities were not manually chosen, but
   there is no qualifying functional outcome. Unspecified IDs alone do not
   establish emergence.
3. **Selection artifact:** positive selection utility is conditioned on the
   selector. Independent endpoint evidence was required and not reached.
4. **Theory-predicted behavior:** optimization of a predictive selection score
   can yield favorable entries. No CDT theorem predicts or rescues a pass.
5. **Scope:** a failed predictive-memory substrate attempt, not a pillar.
6. **Current audit:** the negative survives exact replay, source identity,
   serialization and direct inspection. A positive retention claim does not.

Consequence: retire this fixed continuous-HCM utility selector. No threshold,
support, seed, reward, decoding or capacity adjustment is licensed as a rescue.

## QV1 diagnostics and TAG1 disposition

The QV1 post-mortem is complete: all 160 update rows per arm; seven full
Kaplan-Meier tables; all 1,280 training and 64 held-out initial-state joins;
the frozen three-arm 2x2 at 256 ticks; and per-step resource/energy accounting.
Original greedy episodes reproduce. Every sampled training update used all
six actions, while all original greedy evaluation deaths occurred by tick 56.

On the same held-out worlds, inherited mean age changes from 39.42 greedy to
120.19 sampled; survivors change from 0/64 to 1/64. Reset and fresh policies
show similar lifespan increases. Thus decoding explains much of the observed
lifespan drop without establishing viability or an inheritance advantage.
Future policy contracts must specify decoding and distinguish world from
policy-sampling uncertainty. QV1 is not reopened.

The original QV1 protocol has a disclosed 63-character source-hash typo. The
affected source matches its registration commit after newline normalization;
the other seven source digests match literally. The historical FAIL remains
on record; we do not silently claim a perfected old registration.

TAG1's fixed route is retired by rule. Its result remains qualification VOID:
constant source labels and a detached gradient path prevented a valid test.
No optimizer or endpoint steps were consumed. Revival requires a genuinely
source-varying bank, declared optimizer and gradient path, charter amendment,
and fresh registration. No hotfix or fabricated provenance was applied.

### Concurrent oracle-ceiling evidence

The current checkout also contains the independently committed CYC0 report
(`f58a57c`), which records 64/64 survival through 512 ticks for both scripted
orbit and greedy oracles, versus 0/64 stationary harvest and uniform random.
It is a reported world-solvability witness: its controllers are supplied by
the experimenter, including privileged world access for the greedy oracle.
It does not show a learned policy discovering or retaining a viable cycle,
and it does not alter the retention or pillar verdicts here. Its protocol,
code and report are preserved as concurrent work; this task did not run or
re-adjudicate that campaign. It can motivate a fresh learnability proposal,
but no additional run is licensed by this review.

## Completed capability measurements

The initial saved-report pilot was NOT READY. The user then authorized adding
missing components. POL2 capture reproduced all 64 normal and both fixed-
control episode sets. The logit omission was completed as an explicitly
derived sidecar from saved states and the exact policy; every argmax matches.

CAP1's historical DYN1 capture was VOID when its first feature comparison
exceeded the frozen tolerance. That failure is preserved. CAP1R used fresh
registered seeds 20260970..20260985 and produced exact CPU twins of all saved
state windows and features. This measures the same frozen substrate on fresh
seeds; it does not reproduce or re-adjudicate original DYN1 resilience.

Synthetic covariance/channel checks, all capture hashes, finite metrics,
observed-versus-cloned world transitions, and fixed-reflex separation pass.
Status: **MEASUREMENT COMPONENTS VALIDATED, Class O, no continuation authority.**

| POL2/control | Mean per-world effective actions | Mean viable grid cells | Mean state participation ratio | Mean one-step viable capacity |
|---|---:|---:|---:|---:|
| POL2 normal | 3.757 | 103.61 | 10.300 | 2.565 bits |
| Fixed rest | 1.000 | 12.52 | 0, no controller state | 2.501 bits |
| Fixed harvest | 1.000 | 22.20 | 0, no controller state | 2.526 bits |

The earlier 3.812 repertoire number pooled all decisions; 3.757 here averages
per-world values. These are different declared weightings, not contradictory
results. The mean repertoire difference versus either reflex is 2.757,
paired 95% interval [2.686, 2.817]. Even after matching each world's lifetime
prefix to the shortest of all three policies, POL2 occupies 12.02 more grid
cells than rest [11.23, 12.78] and 6.45 more than harvest [5.72, 7.17].

The capacity values are close to log2(6)=2.585 bits for all controllers.
They measure available one-step body/world transitions, not learned access
to those options. Local response rank is also not ordered by agent quality:
fixed rest averages 4.982 versus POL2's 4.885. These instruments must not become
a scalar proxy for mind, viability, or retained organization.

For the inherited QV1 arm, held-out sampled versus greedy effective actions
are 5.094 versus 1.984, occupied cells 74.48 versus 17.55, and state dimension
2.680 versus 1.462. These measured freedoms coexist with failed viability.
The fresh DYN1-substrate control windows have mean participation ratio 3.358
trained versus 2.541 random. That geometry is not a resilience result. DYN1
has no body/action channel; viability and empowerment are not applicable.

Validated definitions can now be referenced by future registrations. None
earned a next experiment here. A future capability-bar instrument must freeze
its own bar, uncertainty, and the single follow-up protocol before compute.

### Emergence grading of the diagnostic positives

The component status means that its declared calibration checks passed. It
is not a functional retention PASS. The QV1 decoding effect and capability
separation are graded before being admitted as diagnostic evidence:

| Mandatory question | QV1 decoding/energy diagnostics | Capability measurements |
|---|---|---|
| Designed setup? | Yes: frozen learned policies, imposed decoding and engineered body costs | Yes: declared bins, covariance estimator and cloned action channel |
| Unprogrammed setpoint? | Exact lifespans were not specified; no qualifying viability setpoint emerged | Exact statistics were not specified; fixed-controller zero state and singleton repertoire are designed baselines |
| Selection artifact? | Fixed worlds, one specified sampling draw, all three arms; no best-of selection. Final-policy replay is not historical training replay | Predeclared controls and fresh dimension seeds; all captures reported, including the historical VOID. Matched-prefix coverage exposes lifespan bias |
| Theory-predicted anyway? | Sampling changes action support; stationary energy shortage follows the world equations. No CDT conclusion | Finite deterministic channel capacity follows its supplied outputs. Larger covariance dimension is not evidence of self-organization |
| Substrate or pillar? | Failure localization and evaluation-design evidence only | Descriptive, bounded measurement instruments only; Class O |
| Survives current audit? | Yes as a diagnostic with conditional sampling uncertainty and the disclosed old source-hash defect; no retention or viability claim survives | Yes for the declared component calibration and fresh exact twins; no historical DYN1 replay, learned-control, phase or pillar claim follows |

Both positives are engineered/diagnostic observations, not emergent-mind
evidence. All six questions therefore leave the pillar table below unchanged.

## Six-pillar status at closure

| Pillar | Evidence at this review | Status |
|---|---|---|
| Legible expression | Current mouth conceded calibrated P1; no new mouth experiment | Not established; current mouth frozen |
| Causal state authorship | State affects policy actions, but that effect is nonfunctional; speech gate remains closed | Not established |
| Selective memory | Night6 failed; TAG1 qualification VOID; SEL1 failed with 2/8 minimum entries | Not established; current memory route retired |
| Intrinsic resilience | Original DYN1 12/16 versus random 14/16; fresh geometry does not replace the resilience test | Not established |
| Endogenous consequential action | POL2 1/64 through 256 ticks; QV1 greedy 0/64; sampled diagnostic does not satisfy survival bars | Not established |
| Unsolicited initiation | No qualifying expression/action foundation or independent passing initiation contract | Not established; downstream gate closed |

No pillar is upgraded by the capability measurements. The terminal human
session remains gated and was not run.

## Retention exit and explicit strategic decision

| Required successful-handoff obligation | Current evidence |
|---|---|
| Complete generate-select-consolidate-inherit functional loop | Not demonstrated |
| Independent gain over matched no-retention control | Not demonstrated |
| Forgetting/content corruption removes that gain | Not demonstrated |
| Exact reproduction and emergence audit | Execution integrity passes for SEL1, but the function is negative |
| Published artifacts and telemetry | Reports, hashes, local raw artifacts and committed analysis supplied |
| Success doctrine | Not written because its empirical prerequisite failed |

QV0R remains a positive predictive-retention substrate; it is not the complete
loop required for phase handoff. Its named QV1 follow-up has already been used.
TAG1 could not qualify, and SEL1 exhausted the remaining licensed memory route.

**Decision: close this phase with the written negative—functional retention
unachieved with the current tested substrates. Preserve the six-pillar goal;
choose a genuinely new retention-substrate proposal as the only development
route back, rather than narrow the goal to prediction or descriptive freedom.**
No successor compute is licensed by this review. A proposal must name what is
kept, what independently selects usefulness, how it consolidates and inherits,
which matched forgetting/corruption controls establish function, and all
outcome consequences. Commit its complete fresh protocol and source identities
before compute. Retired selectors/controllers cannot be renamed into a new
substrate while merely changing their thresholds, rewards or seeds.

This is a negative closure, not successful phase exit. No higher-pillar work,
mouth tuning or human session is reopened. No phenomenological conclusion is
drawn from either the negative experiments or the positive measurement work.

## Evidence index and completion audit

- SEL1: `zeus_sandbox/universe/reports/sel1_utility_inheritance_20260906.json`;
  both canonical twin directories retain generation, evaluation and tensor
  payloads, with separate logs under `runs/sel1_20260906/`.
- QV1: `docs/qv1_postmortem_20260906.md`, its plotted summary, and the committed
  lossless raw archive `zeus_sandbox/universe/reports/qv1_postmortem_20260906.json.gz`.
- Capability: `zeus_sandbox/universe/reports/capability_completion_20260906.json`;
  every capture path/hash is included. Raw states, logits, channels and dimension
  twins remain under `runs/cap1_20260906/` and `runs/cap1r_20260906/`.
- Registration/defects: original saved-report pilot, CAP1 protocol/manifest,
  logit-derivation note, and CAP1R protocol/manifest are retained separately.
- Concurrent CYC0 witness: `docs/cycle_ceiling_protocol.md` and
  `zeus_sandbox/universe/reports/cycle_ceiling_20260906.json`, read at review
  time and kept distinct from this task's executed experiments.
- Evidence audit: `zeus_sandbox/universe/reports/retention_completion_audit_20260907.json`
  verifies 25 checks and records every retained entry and all four SEL1 bars.

Both supervised jobs exited zero and the final process inventory found no
SEL1 or capability worker. All five plan paths and ratified constitutional
changes are complete. Completion of this execution plan is not completion of
the ultimate six-pillar research objective.
