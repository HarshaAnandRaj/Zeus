# Execution contract: credit, CDT measurement, bounded memory

User authorized this order on 2026-09-12. Adaptive dimensionality is intended to remove dependence on external rescue. A deterministic external gain controller is therefore a calibration aid, not the desired learned self-recovery mechanism.

## 1. Historical credit split

Keep the existing diagnostic selection (revision divisible by 16), original eight QL2 twin-a histories, learning rate and exact final-state checks. For energy-increasing harvests and damaging patch harvests, compute the chosen log-probability gradient through the original chunk. Its contribution to its own ascent direction is advantage times squared score-gradient norm divided by chunk length. Compare that with the projection of the complete sequence actor gradient, separating other transitions algebraically.

Compare actor-only new-gradient updates with historical AdamW, zeroed first moment but retained second moment/counter, entirely fresh AdamW, and plain clipped SGD without decay. The first-moment intervention is the narrow causal comparison; fresh Adam additionally changes preconditioning and bias correction, and SGD additionally changes optimizer and decay. All are local copies, not training recommendations. Counterfactual probabilities replay the same chunk from its fixed incoming state. Report each model and pooled descriptive counts, no independent-transition confidence claims or survival promotion.

## 2. CDT instruments

Preserve the user's existing edits and old outputs. Repair the multivariate phase construction to preserve the complex spectrum's relative phases; test means, cross-spectrum and odd/even endpoints. Reject unknown null names. Treat the phase score as an exploratory contrast and calibrate it on declared stationary controls before any empirical wall claim. Repair the identically-zero capacity probe with an explicit seed-dependent nonzero initial condition; remove claims that it can prove a universal capacity floor. Correct the toy gate-penalty derivative and Adam time counter, verifying relevant gradients before interpreting any earlier numerical prescription.

Do not run adaptive-dimension or novelty-reward training on Zeus during these repairs. Measure recovery of function before calling a future endogenous gate controller self-rescue.

## 3. Bounded memory test

After the first two reports, freeze a separate minimal delayed-information experiment. It must distinguish forward retrieval, delayed writer credit and reader credit with exact identities and a delay beyond the current 64-step truncation. A fixed memory capacity and source/event records are prerequisites, not evidence of authorship. Use independently trained initializations, held-out event sequences, stopped or shuffled credit controls, and a strict functional pass/fail. Passing this engineered assay earns only a later integration experiment; it cannot promote Zeus survival or the six pillars.
