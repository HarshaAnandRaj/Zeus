# Separate reference calibration: prior information and scarce birth resources

Preregistered development-only physical ruler under the full-roadmap authorization.
No neural model, optimizer or held-out learner is used. Current LMB1 default-world
training/source/budget/endpoint stays frozen. This is a new declared world family,
not an alteration of a failed native-function experiment or a replacement metric.

Motivation: LMB1's public teacher can survive256 steps with cleared memory by
reinspecting. A motor PASS would therefore not establish inherited viability
benefit. Here the first body has the original energy.85 and opportunities to
learn; later declared new bodies have resources specified before calibration.
Prior quality information may avoid travel/inspection costs while sensing remains
available. Failure remains evidence against this proposed utility ruler.

Versioned BirthResourceEcology preserves original resource stocks, quality relation,
positions, tool/integrity, action costs/recovery and world/step/RNG rules. Only the
later-body initial energy and its matching declared physical config change. All
original ecology sources remain unchanged. Profiles.12,.20,.35,.85 are fixed and
all retained; no favourable profile is selected after exposure. Four256-step bodies
share the latent relation,32 development ecologies210023000–210023031 per profile.

Public references: carry the safe-side fact learned by inspection in body0;
forget it at each new body and reinspect; probe with HARVEST and use its public
energy consequence; blind-left; blind-right. Public tool estimate starts from the
declared fresh-body initial tool, then updates from observations/executed actions.
No hidden quality/seed/world snapshot enters a reference decision. Observer-only
tool records score physical repairs. Blind routes remain fixed hypotheses, never
oracle corrected. All actions execute as chosen, including costs/fatal outcomes.

PASS requires carry>=90% survival in later bodies at every profile and equal-profile
pooled carry-minus-each-other-reference survival>=.10, lower paired95% bound>0.
Bootstrap10,000 draws, seed210513000, resample complete adjacent ecology pairs,
with all profiles/body cycles/conditions kept together. These are shared ecologies,
not independent observations for each later body. Report survival by profile and
actual feed/inspection counts. Inspection savings never replace a failed survival
gate. Functional calibration PASS/FAIL; source/input/replay defects VOID.

Commit source/contract/tests/protocol before prepare/compute. Manifest freezes
source bytes, config and Git commit. Save complete public physical traces, source
roles and output identity; preserve partial work without automatic retry. Independent
physical/policy replay is required before use as a learning-setting prerequisite.
No repeated seed resampling, energy sweep, threshold change or policy correction.
The independent auditor constructs bodies from the original ecology and changes
only the declared later energy/config fields. It reconstructs every reference
decision without calling the teacher, checks complete ordered public traces and
observer repair fields, and separately reproduces summaries, clustered confidence
bounds and gates. A changed-action corruption check must fail. The manifest also
freezes inherited source/helper dependencies; partial traces are exclusive.

PASS earns a separately preregistered controller-transfer/lineage design on this
family, conditional on audited default motor qualification. The low-energy known-
memory actuator must itself qualify before a learned inheritance endpoint; birth
sensor shift cannot be waved away. No new neural campaign is launched here. Selection,
authorship, endogenous adaptation and the full goal remain separate requirements.
