# LBT1: zero-update known-memory control at scarce-resource birth

Conditional engineered actuator qualification under the full-roadmap authorization.
Building this instrument does not expose an endpoint or establish a capability.
Prepare/evaluate require audited LMB2 motor PASS and its wholly qualified arm,
selected only by LMB2's already frozen learner_history→demonstration priority.
Motor audit must match its actual manifest/verdict hashes. Audited birth-resource
reference calibration PASS is required. All four selected-arm parents stay fixed.
No optimizer, fitting, parent choice or LMB2/older endpoint rerun occurs here.

## Native source and state boundary

Four profiles.12/.20/.35/.85,128 fresh ecologies212113000–212113127 shared across
profiles. Inspected side floor(index/2)%2 balances every side/quality cell32 cases.
Truthful preparation uses the actual versioned BirthResourceEcology at each profile:
body0 starts.85, two moves/one inspection/WAIT through8 steps; bodies1/2 each start
at the declared later energy and execute8 WAITs. Even.12 remains viable at.024 after
those eight decisions. The actual fourth body starts at the same declared profile.
Every observation/reward/cost is real, never relabeled from an original-energy
body. All original world source/quality relation/tool/stocks/integrity/RNG/action
rules are unchanged. Only the already versioned later initial energy/config changes.

Preparation is engineered supplied evidence, not agent-selected acquisition. Only
public transition tensors enter the frozen consolidator. After every source body,
fast/previous-action/reward/start state resets exactly and slow state persists.
Assert first-inspection written slow state equals final inherited state exactly;
no source snapshot, safe-patch label, seed, teacher action or raw cue bypass enters
the evaluated policy. Initialize real continuing fourth-body fast state with that
slow state; observe each actual subsequent action/consequence and eligible write.

The controller gets actual public energy at birth, not the.85 query from an older
source. Raw six-action categorical RNG212413000+1000*parent+index, shared across
matched profiles. Execute every action unchanged until real death or256 decisions.
Save complete action/observation/reward/h/z/probability and observer-only before/
after tool traces. Record first direction and source quality recall before operating.
No safety mask, temperature, teacher, online optimizer or restoration follows a slip.

## Frozen qualification

Every parent×energy×inspection-side×quality cell must satisfy all of:

- Survival through256>=90%, mean positive feeding>=16.
- Mean actual tool-increasing workshop MAINTAIN>=.5.
- Frozen inherited quality reader recall>=90%.
- Exact written/inherited slow identity and complete fast/previous-state resets.

All64 cells,2,048 actual bodies jointly PASS/FAIL. These thresholds are point-estimate
qualification gates; descriptive two-sided95% Wilson survival intervals are reported
with32 shared ecologies per cell, not substituted as stronger confidence claims or
post-exposure bars. Profile/body sharing is not additional independent evidence.
Keep default-energy cells and every scarce profile. First-direction or inspection
metrics cannot rescue failed full-operation gates. Repair execution is not proof
that repair is necessary. Corruption, incomplete source/twin prerequisite, coverage
or replay defects VOID. No UNDECIDED, favourable-profile choice, budget/horizon
change or training after transfer exposure.

## Independent instrument and receipts

Commit all new runner/auditor/tests/protocol and source dependencies before prepare.
Manifest freezes selected exact candidate hashes, resource/motor audit identities,
versions, source bytes and Git commit. Exclusive artifacts preserve partial work.
Use prevalidated JSON from the independently tested new serializer; no old partial
campaign file is overwritten. Mechanics fixture candidates are labeled unqualified
and never become transfer endpoint evidence.

Independent auditor starts from original LineageEcology, applies the declared energy/
config changes itself, replays all24 preparation actions and actual fourth-body
NumPy GRU/gate/actor/sampling/public consequences/eligible slow writes. Probability
2e-5, state1e-4, sampled actions/physical summary fields exact, quality1e-5.
Storage/reset checks are measured from actual states. Independently reproduce every
cell/gate, Wilson intervals within1e-12 and coverage/identity. Verify committed source,
full ordered traces and fixed parent parameters. No gradient/optimizer capability
is claimed because zero-update transfer has no fitting.

Audited PASS earns a fresh frozen own-experience native inheritance experiment,
with acute erasure, opposite content, disabled writes and matched trained forgetting.
Before benefit claims, that design must check that actual source inspections write
usable facts and that each allowed controller can operate under its information
condition. A supplied-source actuator pass does not establish learned acquisition,
selective retention, native inherited viability benefit, authorship or a full pillar.
FAIL earns read-only sensor/state diagnosis and, if justified, fresh equal-profile
learning with new roles/endpoints; it never reopens LBT1 or forces a memory conclusion
onto actuator failure. Integrated long operation and all remaining roadmap stay owed.
