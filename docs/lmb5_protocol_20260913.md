# LMB5: prospective equal-budget scarce-birth motor learning

Motivation: independently audited LBT2FAIL and its complete frozen diagnosis.
This new campaign cannot alter or promote the exposed zero-update endpoint.
Engineered training exposure is allowed. Immediate-feeding teacher targets are
explicitly taught; this is an actuator prerequisite, not an emergence verdict.

## Frozen source, conditions and roles

Source: all four independently qualified LMB4 grounded models/heads, bound through
the complete source guard, LBT2 audit and complete diagnosis/source hashes. Keep
store/quality and prediction heads frozen. Heads never choose actions or receive
new optimization. Train only fast/gate/reinstatement/actor. No hidden seed,
quality, resource vector or observer physical field enters actor/teacher decisions.

| Role | Ecology base | Ecologies | Conditions |
|---|---:|---:|---|
| Development only | 992000/993000 families | 4 | Short16-step fixtures; excluded from campaign |
| Training | 219013000 | 64 | Four slots × inherited/empty,512 teacher bodies per arm/twin |
| Public teacher check | 219063000 | 64 | Same condition structure,1024 bodies across both arms |
| Fresh endpoint | 219113000 | 128 | Both arms all inherited profiles plus empty at.85; warm all inherited profiles |
| Separate native readout | 219123000 | 128 | All four profiles, full/reset/real opposite banks |

Arms in frozen selection order: balanced then original. Four nominal profile slots
are.12/.20/.35/.85. Balanced uses those actual initial energies; original uses
actual.85 in every slot, repeating the same high-energy examples rather than
claiming four independent worlds. Within a slot use index0..63 and inherited then
empty. Public source preparation: three actual8-step bodies; source0 gets two
supplied moves/INSPECT thenWAIT, later sourcesWAIT. Only declared birth energy is
changed, all initial integrity/tool/resources/config/RNG retained. Slow state
derives only from actual public records; evaluated fast/action context resets.

## Reachability before learning

Generate the fixed public teacher check and run the independent calibration audit
before any fit. Known-memory teacher must survive all64 bodies in every actual
profile/arm. Empty-memory teacher must survive all64 except actual.12, where32/64
is expected: public left inspection succeeds when that patch is safe and cannot
fund the full wrong-side detour otherwise. The original arm's slots all have
actual.85, so all teacher bodies survive. Teacher is the existing public controller;
the independent auditor arranges its rule separately and replays all public
preparations/physics/scalar balances. Any reachability mismatch yieldsVOID before
learning; do not alter teacher, profiles or thresholds after seeing it.

Raw calibrationPASS alone cannot launch training. Require its independently
auditedPASS and exact manifest/public-data/verdict hashes. Actual empty-scarce
teacher deaths are expected data, not padding that supplies fictional transitions.

## Fixed learning and reproducibility

For each arm/parent, execute exact whole-model/head/Adam/input/log twins a and b.
96 updates, batch8: one randomly selected ecology in each profile-slot/context
pair using the same fixed indices across arms/twins, batch seed219313000+update.
Learning rate.003, action weights[1,8,8,4,16,16], native full-memory first-direction
cue weight.25, clip1, truncation32. Body objective is weighted active teacher-label
cross-entropy; native cue derives from the supplied public source inspection.
Forward recurrent state continues through each256-step trajectory; backward
history detaches every32 decisions. Published live-step counts distinguish equal
updates from unequal active examples due to real cold empty-memory deaths.

Verify independent public encoding, every minibatch ID/hash/live count, positive
first body gradients in all four intended modules, zero frozen memory/quality
credit, changed active parameter hashes, unchanged frozen modules/heads, revision
increment96, and exact full checkpoint/optimizer/log twin identities. This supplies
reproducibility and causal-route checks, not a separate independent Adam engine.

## Fresh raw operation and qualification

Evaluate all four parents, raw softmax sampling, fixed weights, no teacher,
action override/mask/planner, optimizer, state rescue or extra reset within a body.
Horizon256; generator219413000+1000*parent+index, paired across variants, profiles
and memory modes. Fitted arms each evaluate640 bodies/parent:512 inherited plus
128 empty at.85. Zero-update warm reference evaluates512 inherited/parent.
Total7168 bodies. Explicit inherited/empty starts; later public sensing/writes stay
available. All physical/neural/public records are retained with complete coverage.

Each fitted parent/profile/inspected-side/quality cell has32 bodies. Require at
least29/32 survive, mean positive-net feeding>=16, actual tool increases>=.5,
inherited initial quality recall>=.90, exact storage identity and fast reset.
For empty.85 cells apply survival/feed/repair and zero-start/reset checks; empty
quality/direction are diagnostic. Every80 body cell must pass for that arm.
First direction and learned strategies remain diagnostic, not substitutes for
viability. No pooled survival, extra time or profile omission rescues a cell.

Separate native readout uses512 public preparations, three controls and each
fitted parent:12288 queries. Full content must yield>=.90 sampled correct direction
and decoded inspected quality in each parent/profile/side/quality cell. Opposite
content comes from the real public preparation at same-profile index XOR1, with
the same inspected side/opposite quality; require>=.90 direction/quality against
that donor's public content. Reset is acute zero state. Full-minus-reset direction
benefit per energy requires point>=.40 and95% lower bound>0. This is a readout
contrast, not the owed native survival-memory contrast. No synthetic-regression
result is attached to newly fitted models.

## Attribution, audit and purchase

Report balanced-minus-original inherited survival separately at.12 and.20.
Exposure attributionPASS requires each point>=.02 and95% lower bound>0. Report
both fitted-minus-warm contrasts descriptively. Bootstrap10000 draws, seed219513000,
four parents with replacement and32 ecology quartets with replacement, preserving
side/quality cells and cross-energy pairing. Bounds describe this finite family.

An arm's actuator qualification does not depend on exposure attributionPASS.
Select the first wholly qualified arm in frozen order; no arm qualifies if none
passes. A qualified original arm or attributionFAIL is published honestly.

Independent final audit binds complete source/check/data/model/report identities,
audited reachability, all8 exact fit pairs/768 minibatches, public source routes,
all7168 full CPU neural/scalar physical trajectories and12288 readout queries.
Saved h/z are assertions, not replayer state inputs. Require exact shared-PyTorch
hardware trajectory and local NumPy probability<2e-5/state<1e-4, independent quality
math<1e-5 and scalar balances1e-12. No engine portability/independent Adam claim.

Audited actuatorPASS earns preparation of the separately frozen agent-own
acquisition/native inheritance experiment. It does not establish useful inheritance,
learned selection, authorship, internal priorities, life or a six-pillar result.
Acute erasure/opposite content/disabled writes/matched trained forgetting and all
remaining roadmap obligations stay owed. FunctionalFAIL earns complete diagnosis
before any genuinely justified fresh mechanism. Integrity defects yieldVOID;
preserve artifacts and do not repair an exposed source to rescue it. Commit all
source/protocol before preparation and compute; no post-exposure tuning.
