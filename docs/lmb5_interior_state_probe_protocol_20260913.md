# LMB5 fixed-reading causal contribution probe

Read-only follow-up to the complete version2 diagnosis and independently audited
FAIL. Freeze this source before compute. Use all four balanced controllers and
every interior-position decision, including both memory modes, all energies,
side/quality cells and original survival outcomes. Stratify by original argmax
WAIT versus other actions; empty groups remain explicit. No selected best parent.

Reconstruct each decision's pre-action fast/slow state and previous-action/reward/
boundary inputs from its audited trace. Replay intact current-reading logits in
batches of at most1024 and require probability error<2e-5. Compare intact,
zero-fast, zero-slow and both-zero while holding public readings and all other
inputs fixed. Report teacher argmax agreement/probability, WAIT probability,
argmax switches and distribution L1 differences with explicit denominators.
Teacher state uses only the supplied public cue for inherited bodies and actual
public inspections for empty starts. No teacher acts in the physical world.

Zeroing may create off-distribution states. An immediate distribution difference
establishes a contribution in these readings, not memory absence, a dynamical
attractor, motivation, sustained recovery or a functional pass. No changed action
executes; no model/world update occurs. Bind all artifacts/source/diagnosis hashes
and verify model identity before/after. Preserve any existing report.
