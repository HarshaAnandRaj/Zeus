# Offline public-state availability in fast state and mouth

Diagnostic only, conditional on audited LMB3 functional FAIL. Commit before
compute. All1,024 candidate bodies, including34 deaths; audited source hashes
must match. No model update, replacement action, free-run rescue or new functional
qualification. Reconstruct mouth vector h+gate(h,z)*reinstate(z) using actual
same-step fast state and pre-action slow state, and verify its output against
recorded probabilities within2e-5. Public targets are current energy/integrity/
position (observation indices0–2), never hidden world state.

Separate fixed linear probes for h32, mouth32 and z8 in every parent/mode. Fit
on ecology indices0–63, evaluate64–127, preserving shared adjacent pairs and
all records per ecology. Standardize each feature using training mean/std only;
constant scales become1. Mean-squared ridge lambda.001 on standardized features,
unpenalized bias, no tuning/cross-validation, no prediction clipping. Report
held-out RMSE/MAE for all three sensors and nearest-public-position accuracy
on {0,.25,.5,.75,1}. Report all, fatal, survivor and interior WAIT-argmax groups
with counts. Empty selections are n=0, never a favourable score.

Different records are dependent, so no independent sample/confidence claim.
Linear availability does not establish causal usage by the actor. Poor linear
decoding cannot prove information is absent; nonlinear readers may recover it.
Mouth/fast comparisons may motivate a new experiment, not choose a favourable
parent or change the closed qualification. Probe parameters and exact identities
are preserved for reproducibility. No thresholds license continuation or a pillar.
