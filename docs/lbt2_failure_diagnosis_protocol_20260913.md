# LBT2 complete scarce-birth failure anatomy

Read-only diagnosis conditional on a complete independent LBT2 audit PASS that
confirms the frozen functional FAIL. Freeze this protocol and its extractor before
extraction. Preserve all four parents, all four initial-energy profiles, both
inspected sides/qualities, all2048 actual bodies and every fatal body. No endpoint
threshold, source weight, sampler, preparation or episode is changed.

Bind the audited manifest, preparation, whole public trace, endpoint, verdict,
report and source identities. Independently reconstruct all512 public source
preparations and each evaluated physical body, compare every public consequence,
observer before/after physical vector and scalar balance. The existing independent
audit supplies complete neural replay; this extractor makes no second neural,
optimizer or portability claim. Recorded neural values remain observations.

For every body, record initial quality correctness, storage identity, first
sampled action/argmax/probability of the remembered safe direction, and the first
eight actual decision records. Count all actions, interior actions, first-eight
interior actions and fixed-public-teacher agreement for sampled actions and
argmaxes. Teacher state starts only from the supplied public inspection and
updates through actual action/public consequences. Privileged tool, physics and
seed fields never choose teacher actions.

Record elapsed steps to first safe/unsafe patch arrival, first safe harvest,
first positive net feeding, first inspection and first slow-state change. Do not
confuse visiting a patch, extracting safe food and positive net energy gain.
Record actual feeding/tool-restoration/inspection/contamination counts and verify
the original endpoint summaries. Record last positive feed/repair gaps, terminal
energy/integrity/position/action and joint death categories. Preserve complete
last16 public/neural decision records for every death. Slow-state change can be
an appropriate public write; it is not automatically memory loss.

Aggregate every parent/energy/side/quality/outcome group, including empty groups,
without pooling away a failed cell. Pooled profile summaries are descriptive.
Initial quality recall is the existing decoder measurement, not proof that the
controller uses the fact. Teacher agreement is not an optimality or viability
result. No replacement action, neural state intervention, teaching update or new
counterfactual body runs in this observer.

The result may justify a fresh equal-profile controller-learning protocol with
separate fit/check/evaluation roles. It cannot promote LBT2, earn own acquisition,
establish native memory benefit or close another roadmap requirement. Acute
erasure, opposite content, disabled writes and matched trained forgetting remain
owed in their separately frozen functional experiment.
