# LMB2: learner-history motor correction with an equal-budget demonstration control

Fresh protocol under full-roadmap authorization, based on LMB1's closed VOID and
independently verified frozen-rule functional FAIL. This is a new negative-driven
mechanism test, not an earned higher-pillar transition, old continuation or reporting
repair. All four LMB1 candidates start both arms identically; no parent selection.
Original LMB1 source, partial verdict, training budget and endpoint stay unchanged.

## Hypothesis and mechanism

LMB1 often prefers WAIT/HARVEST between patches after deviations from teacher routes.
Its teacher corpus contains no such states/actions. Test whether supervised labels
on actual learner-induced histories improve raw body competence beyond an equal
optimizer budget of demonstrations. The unchanged teacher sees only current public
readings and its own remembered public inspections/action-based tool estimate.
Teacher labels never select or replace executed collection/evaluation actions.

All four continuing fast GRU/projection/gate/actor modules train; original protected
store and quality reader remain exact frozen prerequisites. No action mask, cue
bypass, hidden quality, world snapshot, seed or label enters the policy input.
Observation/previous executed action/reward/boundary feed continuing fast state.
Actual inspections can update slow state.32-step BPTT detaches without resetting.
Gradient/module movement and action→physics→next public observation→state are audited.
This is public-supervised correction, not reward-based autonomous learning, selected
memory, discovered goals, or complete self-authorship.

## Fixed fitting and roles

Fresh training128 ecologies211013000–211013127, both inspection preparations,
256 demonstrations using original24-step three-body public preparation/fourth body.
Known/empty assignments and quality/side balance follow the unchanged LMB1 generator.
Separate public teacher calibration128 ecologies211023000–211023127 must produce
complete surviving256-step trajectories; otherwise VOID before fitting.

Two arms: learner_history and demonstration.96 fresh Adam updates lr.003, batch8,
norm cap1, old action weights1/8/8/4/16/16 and.25 auxiliary cue loss,32-step BPTT.
Restart Adam identically from each LMB1 candidate; revision384→480. CPU float32,
one thread, deterministic algorithms, default Adam settings, foreach/fused false.
No validation choice, early stop, retry, continued fitting or loss/budget sweep.

Before updates0/12/.../84, freeze each arm's current model/optimizer snapshot and
collect8 actual raw bodies from demonstration indices8*refresh..8*refresh+7. Each
slice balances known/empty, both qualities and inspected sides. Both arms collect
and query public labels, but only learner_history fits that corpus. Action RNG
211343000+100000*parent+1000*refresh+case. Death ends the body and remains recorded.
No exclusion, policy substitution or continuing dead world is permitted.

Learner_history aggregates all prior collected histories. Each optimizer batch has
four fixed demonstrations plus four aggregated learner histories. Demonstration
uses four plus four fixed demonstrations. Both have96 identical-size update slots,
identical cue objective and collection workload; live transition counts may differ
and must be reported. Batch draws Torch211313000+update, first four then second four.
Variable dead histories are explicitly padded to256 only for batching; padding is
inactive in the weighted CE numerator/denominator and never an executed world step.
Fast recurrence replays each actual executed history from the declared initial state.
No externally stored hidden snapshot substitutes for current recurrent computation.

Auxiliary native queries use the eight selected histories' original public
preparations, full cue state and first-direction targets. Synthetic cue input
211033000+update, batch8, block1. Half native/half synthetic CE as in LMB1; this is
declared prerequisite supervision, not autonomous acquisition.16 complete fits:
four parents×two arms×two exact model/optimizer/input/log/collection twins.

## Fresh raw function and preserved-memory endpoint

After every complete twin pair,128 ecologies211113000–211113127 with original
balanced preparation side floor(index/2)%2. Each corrected arm, unchanged LMB1 warm
candidate and original LCM5 initial body policy acts in inherited/acute-empty modes.
References are executed once and shared across arms; shared observations are not
new independent samples. Raw six-action RNG211413000+1000*parent+index, full256
physics decisions or actual death. No teacher/optimizer/filter at endpoint.

Each arm separately satisfies all original LMB1 qualification bars: every parent/
mode survival>=90%, mean positive feeding>=16, mean physical tool-increasing workshop
repair>=.5; pooled gain over original initial policy>=.50 with positive lower paired
95% bound; native/delayed content regressions below all pass. Repair execution still
does not establish repair necessity. Warm-candidate gains are descriptive; no old
exposed-data score substitutes for the fresh references.

Native regression256 ecologies211123000–211123255, both sides, original three resets:
every sampled direction/quality cell>=90%, donor-follow>=80%, exact storage/reset,
full-minus-erasure>=.30 and full-minus-opposite>=.60, positive lower paired bounds.
Synthetic512 paired cues at64/128 blocks, input211223000+block, action211423000+
10000+1000*parent+block; original>=90% direction/quality,>=80% donor, exact storage.
Native action211423000+parent. Bootstrap10,000, seed211513000, complete adjacent
ecology pairs and parent draws, preserving matched sides/arms/body modes.

Grade data-source attribution separately: pooled learner_history-minus-demonstration
survival>=.02 and lower paired95% bound>0 in both modes. This earns an attribution
PASS/FAIL only. Motor qualification is separate: if learner_history wholly qualifies,
use it; otherwise if demonstration wholly qualifies, use that arm; otherwise none.
That priority is frozen now, never best-parent or CE selection. Control qualification
can demonstrate usable correction even if the learner-history hypothesis FAILS.

## Integrity and consequences

Commit every new source/contract/runner/auditor/test/protocol before prepare/compute.
Manifest freezes all source dependencies, exact LMB1 candidates and diagnostic audit.
JSON is fully type/finite validated before opening exclusive artifacts; NumPy scalar
roundtrip and invalid-object/no-file checks must pass before freeze. Preserve partial
work without reruns. Functional PASS/FAIL; corrupt sources/inputs/twins/replay VOID.
No UNDECIDED or post-exposure rescue. Complete frozen-source Git verification,
teacher/preparation/learner-label public physics, actual raw NumPy actions/fast state/
slow writes, full twins/snapshot/input/batch identities, regressions, metrics and
independent gates/CIs are required. Probability2e-5/state1e-4; actions/metrics exact.
Optimizer twins/gradient tests do not constitute independent numerical Adam replay.

An audited motor PASS earns fresh known-memory actuator transfer qualification in
the already separately calibrated birth-resource family, followed only on transfer
PASS by a new frozen inherited-function experiment with acute reset/opposite/write-
disabled/matched trained forgetting controls. No inherited-function or pillar pass
follows from this correction. Failed arms stay closed and get read-only diagnosis.

Literature mechanism: Ross/Gordon/Bagnell,AISTATS2011, [primary paper](https://proceedings.mlr.press/v15/ross11a/ross11a.pdf),
Algorithm3.1 aggregates expert labels on policy-induced trajectories. Our fixed-final
raw recurrent/truncated implementation uses that empirical idea; paper validation
selection and theoretical convex/no-regret assumptions do not license a success
guarantee here. Public labels replace any privileged planner; no paper reproduction
or reward-based discovery is claimed. Undermind full text Ros10 read2026-09-13.
