# Does the present world require repair? Public-reference counterexample

Fresh diagnostic only; no LMB4 source, budget, horizon or gate changes. Repair
counts remain its declared qualification bar. Roadmap item1 separately requires
meaningful repair-dependent long operation. Inspecting the original physics
suggests that requirement is not guaranteed merely by extending a run.

At tool0, a full safe harvest gains .13*.35-.012-.002=.0315 energy, while a WAIT
costs .012 and, above .50 post-metabolism energy, gains .005-.002=.003 integrity.
These are local configured balances, not yet a trajectory or infinite-horizon proof.

Freeze32 fresh static lineages215063000..031, original body0 at energy.85,
4096 steps. Two public reference policies: passive WAIT; no_repair first moves
left twice, inspects, remembers the public quality/safe-side relation, travels
to the safe patch, then HARVEST if energy<.65 and WAIT otherwise. Neither
uses a snapshot, seed, hidden quality or private tool state to select actions.
No MAINTAIN action, neural model, optimizer or inherited state is used.

Persist every public action/observation/death and observer-only physical before/
after record. Freeze runner/protocol/dependency identities before compute;
exclusive output runs/repair_necessity_counterexample_20260913. No threshold,
policy, seed or horizon sweeps if the counterexample is absent.

Separate audit reconstructs each public policy from actual observations and
the seeded world, replays every physical/public transition, checks independently
arranged scalar quality/energy/integrity/tool/resource balances at1e-12, exact
death/coverage/counts and evidence hashes. Existing frozen balance helper is used.
It shares the world implementation for exact replay; independent scalar accounting
is explicit. Audit integrity defect invalidates the diagnosis.

One full surviving no-repair body with positive time at tool0 is a finite-horizon
counterexample: repair is not necessary for that viable operation. Report all32
cases and passive results. Absence of such a body means only no counterexample
detected by this one policy, not proof of necessity. No learned maintenance,
emergence or pillar pass follows. A future repair-dependent family would need
separate calibrated physics and controls; this diagnostic earns no alteration
or rescue of any existing campaign.
