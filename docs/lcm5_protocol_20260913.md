# LCM5: a native-public quality reader on fixed memory and action parents

Preregistered repair after audited LCM4-C compatibility FAIL. The active user goal
authorizes work through the full roadmap. LCM4-C remains closed FAIL; LCM3 remains
closed PASS. No thresholds, sensors, original parent tensors or old verdicts change.
This is engineered supervised readout qualification, not native viability, selective
memory, endogenous acquisition or a pillar result. PASS earns motor construction
and a separately frozen body-control qualification; it never licenses skipping it.

## Fixed mechanism, budget and roles

All four LCM3 raw-gated parents and their exact original consolidators/actors are
fixed. A fresh linear8→2 reader reports public good/bad quality; the inspected side
chooses the trained output in the loss. Side/labels never enter the reader input
or actor. No base optimizer continuation or base gradient. Each parent gets equal
initial readers in inherited and no-write arms, seed208313000+parent. The no-write
reader trains on raw zero memory. There are eight unique runs and eight exact twin
pairs; full model/optimizer/initial-reader/input/log payloads must match.

Training corpus:512 ecologies208013000–208013511 at both inspected patches. Use
actual24-step public three-body sequences from the committed LCM4-C adapter and
frozen store, including true costs/reward/quantity. Full slow state is detached
after consolidation. Dataset is fixed and shared across parents/arms;1024 examples
are not24,576 independent ecologies. Regenerate the complete corpus in each twin.

384 updates, minibatch64 sampled with replacement by Torch seed208513000+update.
Adam lr.03, default betas/epsilon, foreach/fused false, gradient norm cap1, binary
cross-entropy on the publicly inspected quality output. Only new reader parameters
train. CPU float32, one thread, deterministic algorithms. No validation selection,
early stopping, extra budget, quantity clamp or threshold adjustment.

Reserve development208023000–208023127 for later diagnostics only; no fitting or
selection on this role. Qualification512 fresh ecologies208113000–208113511, both
patches, same24 physical steps and three body resets. Synthetic regression512 paired
cues each at64/128 blocks (192/384 nuisance steps), seeds208213000+block. Neither
old exposed LCM4 data nor LCM3 endpoints/diagnostics enter training or the new test.
Initial reader and unchanged original quality head remain diagnostic references.
No material reader-necessity attribution is claimed from the original-head contrast.

## Native binary qualification

Unchanged raw six-action categorical sampling, RNG208413000+parent shared across
full/reset/opposite conditions. Acute zero is raw memory zero; opposite donor uses
the adjacent opposite ecology at the same inspected side (XOR2). All original LCM4-C
action/recall/identity/donor gates apply using the new reader, with unchanged90%
per-parent/patch/quality bars, donor action-follow80%, full-reset margin.30,
full-opposite.60 and strictly positive lower95% paired bootstrap bounds.

Additional gates prevent a quantity-conditioned failure hiding in averages:

1. Each parent/patch/quality cell must achieve>=90% recall separately at both
   pre-inspection coarse bins0.5 and0.75. Require>=32 physical examples per bin;
   missing coverage is VOID, with no seed resampling.
2. Full-reader native quality accuracy minus acute-zero and matched trained-no-write
   quality accuracy each>=.30; minus opposite-content>=.60. All lower95% bounds>0.
3. On the reserved delayed synthetic regression, every parent/patch/quality cell
   at both lengths has>=90% sampled action and new-reader recall; opposite donor
   action-follow>=80%; storage remains exact. No source-distribution regression is
   traded away for a native-only head.

Native bootstrap10,000 draws, seed208613000, resamples parents and complete adjacent
two-ecology/two-patch clusters with common draws. Synthetic action RNG208423000+
1000*parent+block. References use their fixed heads and do not select the primary.
All functional gates yield joint PASS/FAIL. Source/parent/twin/input/coverage/replay
defects yield VOID. No UNDECIDED, pooling away cells, head selection or post-hoc rescue.

## Integrity and next scope

Commit clean source/tests/contract/protocol before prepare. Manifest freezes exact
source bytes, Git commit, package versions, parent checkpoint/model/logical hashes
and previous audit. Preserve partial artifacts without automatic retry. Training
completes and twins qualify before endpoint exposure. Independent audit replays
real public physics, labels, source identity, all frozen parent tensors, corpus and
minibatch inputs; reconstructs linear-reader BCE/gradients/Adam in NumPy; reconstructs
native/synthetic actor and reader probabilities, sampling, verdict and bootstrap.
Numerical audit tolerances: reader parameter2e-4, loss/norm2e-5, endpoint1e-5.

If PASS, build a body-session actor with within-life fast recurrence and protected
memory, bounded public motor demonstrations as an explicit prerequisite, missing-
memory acquisition cases and256-step raw native viability with feeding/passive
controls. Freeze its exact numerical protocol before motor compute. If FAIL,
diagnose the separately trained reader/representation without extending budget or
unfreezing the parent; the full roadmap remains open.
